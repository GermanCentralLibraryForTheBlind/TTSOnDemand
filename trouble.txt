dp2 won't come up 
- Delete the this key? Shutting down the framework because of:/tmp/dp2key.txt (Keine Berechtigung) 

client make curios things?
- is bundle uptodate -> npm run build-bundle

daisy pipeline 2 No 'class' in configuration for px:inline-css

- delete jobs
- restart dp2


No 'class' in configuration for px:break-detect

- switch to java 7